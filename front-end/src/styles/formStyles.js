import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    input: {
        marginTop: 10,
        marginBottom: 5,
        paddingVertical: 10,
        paddingHorizontal: 16,
        borderWidth: 1,
        borderColor: '#ddd',
        borderRadius: 6,
    },
    errorText: {
        color: 'red',
        fontWeight: 'bold',
        marginBottom: 10,
        textAlign: 'center',
    }
})